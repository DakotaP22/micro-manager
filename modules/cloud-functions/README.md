# cloud-functions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build cloud-functions` to build the library.

## Running unit tests

Run `nx test cloud-functions` to execute the unit tests via [Jest](https://jestjs.io).
